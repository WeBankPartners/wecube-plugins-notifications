{
  "http": {
    "port": "9095",
    "token": "default-token-used-in-server-side"
  },
  "mail": {
    "enable": true,
    "sender": [
      {
        "protocol": "smtp",
        "name": "wecube-notification",
        "user": "{{MAIL_AUTH_USER}}",
        "password": "{{MAIL_AUTH_PASSWORD}}",
        "token": "",
        "server": "{{MAIL_AUTH_SERVER}}"
      }
    ]
  }
}